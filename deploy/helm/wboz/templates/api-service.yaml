apiVersion: v1
kind: Service
metadata:
  name: {{ include "wboz.apiServiceName" . }}
  labels:
    {{- include "wboz.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "wboz.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
